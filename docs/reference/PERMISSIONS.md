# Permissions matrix

> **Generated by `scripts/Build-PermissionsMatrix.ps1`. Do not edit by hand.**
>
> Sources:
> - `src/M365-Assess/Orchestrator/AssessmentMaps.ps1` (services + delegated Graph scopes per section)
> - `src/M365-Assess/Setup/PermissionDefinitions.ps1` (app-only Graph permissions, EXO RBAC role groups, Purview/Compliance directory roles)
>
> CI runs `Build-PermissionsMatrix.ps1 -Check` and fails any PR that modifies the source maps without regenerating this doc.

## Overview

| Section | Services | Delegated scopes | App permissions | EXO RBAC groups | Purview/Compliance roles |
|---|---|---|---|---|---|
| ActiveDirectory | — | 0 | 0 | 0 | 0 |
| Collaboration | Graph | 4 | 4 | 0 | 0 |
| Compliance | — | 0 | 0 | 0 | 1 |
| Email | ExchangeOnline | 0 | 1 | 1 | 0 |
| Hybrid | Graph | 2 | 2 | 0 | 0 |
| Identity | Graph | 9 | 8 | 0 | 0 |
| Intune | Graph | 2 | 2 | 0 | 0 |
| Inventory | Graph, ExchangeOnline | 7 | 7 | 1 | 0 |
| Licensing | Graph | 2 | 2 | 0 | 0 |
| PowerBI | — | 0 | 0 | 0 | 0 |
| Security | Graph, ExchangeOnline, Purview | 1 | 4 | 2 | 3 |
| SOC2 | Graph, Purview | 8 | 0 | 0 | 0 |
| Tenant | Graph | 5 | 5 | 0 | 0 |
| ValueOpportunity | Graph | 1 | 0 | 0 | 0 |

## Section detail

### ActiveDirectory

**Services touched:** None — local-only collector

### Collaboration

**Services touched:** Graph

**Delegated Microsoft Graph scopes**

- `OrgSettings-Forms.Read.All`
- `SharePointTenantSettings.Read.All`
- `TeamSettings.Read.All`
- `TeamworkAppSettings.Read.All`

**Microsoft Graph application permissions** (app-only auth)

| Permission | Reason |
|---|---|
| `OrgSettings-Forms.Read.All` | Microsoft Forms tenant-level settings |
| `SharePointTenantSettings.Read.All` | SharePoint and OneDrive tenant-level settings |
| `TeamSettings.Read.All` | Teams tenant-level settings and policies |
| `TeamworkAppSettings.Read.All` | Teams app permission and setup policies |

### Compliance

**Services touched:** None — local-only collector

**Entra directory roles** (for Purview / Compliance access)

| Role | Reason |
|---|---|
| `Global Reader` | Broad read-only access across Microsoft 365 services including Purview, covering gaps not addressed by the above roles. |

### Email

**Services touched:** ExchangeOnline

**Microsoft Graph application permissions** (app-only auth)

| Permission | Reason |
|---|---|
| `MailboxSettings.Read` | Mailbox-level settings (forwarding, audit, locale) via Graph |

**Exchange Online RBAC role groups**

| Role group | Reason |
|---|---|
| `View-Only Organization Management` | Read-only access to mailboxes, recipients, transport rules, connectors, and EOP/Defender policies. Replaces the on-prem-only "View-Only Recipients" and "View-Only Configuration" groups in cloud EXO. |

### Hybrid

**Services touched:** Graph

**Delegated Microsoft Graph scopes**

- `Domain.Read.All`
- `Organization.Read.All`

**Microsoft Graph application permissions** (app-only auth)

| Permission | Reason |
|---|---|
| `Domain.Read.All` | All domains registered in the tenant |
| `Organization.Read.All` | Tenant org details, verified domains, hybrid config |

### Identity

**Services touched:** Graph

**Delegated Microsoft Graph scopes**

- `Agreement.Read.All`
- `Application.Read.All`
- `AuditLog.Read.All`
- `Directory.Read.All`
- `Domain.Read.All`
- `Policy.Read.All`
- `RoleManagement.Read.Directory`
- `User.Read.All`
- `UserAuthenticationMethod.Read.All`

**Microsoft Graph application permissions** (app-only auth)

| Permission | Reason |
|---|---|
| `Application.Read.All` | App registrations, service principals, OAuth permission grants |
| `AuditLog.Read.All` | Sign-in logs and directory audit events |
| `Directory.Read.All` | Devices, admin units, role templates |
| `Domain.Read.All` | All domains registered in the tenant |
| `Policy.Read.All` | Conditional Access, auth methods, token lifetime, password policies |
| `RoleManagement.Read.Directory` | Entra directory role assignments and PIM eligibility |
| `User.Read.All` | User profiles, sign-in activity, license assignments |
| `UserAuthenticationMethod.Read.All` | MFA and passwordless authentication methods per user |

### Intune

**Services touched:** Graph

**Delegated Microsoft Graph scopes**

- `DeviceManagementConfiguration.Read.All`
- `DeviceManagementManagedDevices.Read.All`

**Microsoft Graph application permissions** (app-only auth)

| Permission | Reason |
|---|---|
| `DeviceManagementConfiguration.Read.All` | Configuration profiles, compliance policies, Multi-Admin Approval policies |
| `DeviceManagementManagedDevices.Read.All` | Managed device inventory and compliance state |

### Inventory

**Services touched:** Graph, ExchangeOnline

**Delegated Microsoft Graph scopes**

- `Channel.ReadBasic.All`
- `Group.Read.All`
- `Reports.Read.All`
- `Sites.Read.All`
- `Team.ReadBasic.All`
- `TeamMember.Read.All`
- `User.Read.All`

**Microsoft Graph application permissions** (app-only auth)

| Permission | Reason |
|---|---|
| `Channel.ReadBasic.All` | Teams channels |
| `Group.Read.All` | All groups including Microsoft 365 and security groups |
| `Reports.Read.All` | Microsoft 365 usage reports |
| `Sites.Read.All` | SharePoint site enumeration and metadata |
| `Team.ReadBasic.All` | Enumerate all Teams |
| `TeamMember.Read.All` | Teams membership details |
| `User.Read.All` | User profiles, sign-in activity, license assignments |

**Exchange Online RBAC role groups**

| Role group | Reason |
|---|---|
| `View-Only Organization Management` | Read-only access to mailboxes, recipients, transport rules, connectors, and EOP/Defender policies. Replaces the on-prem-only "View-Only Recipients" and "View-Only Configuration" groups in cloud EXO. |

### Licensing

**Services touched:** Graph

**Delegated Microsoft Graph scopes**

- `Organization.Read.All`
- `User.Read.All`

**Microsoft Graph application permissions** (app-only auth)

| Permission | Reason |
|---|---|
| `Organization.Read.All` | Tenant org details, verified domains, hybrid config |
| `User.Read.All` | User profiles, sign-in activity, license assignments |

### PowerBI

**Services touched:** None — local-only collector

### Security

**Services touched:** Graph, ExchangeOnline, Purview

**Delegated Microsoft Graph scopes**

- `SecurityEvents.Read.All`

**Microsoft Graph application permissions** (app-only auth)

| Permission | Reason |
|---|---|
| `DeviceManagementApps.Read.All` | Intune audit events including device wipe/retire/delete actions |
| `DeviceManagementConfiguration.Read.All` | Configuration profiles, compliance policies, Multi-Admin Approval policies |
| `DeviceManagementRBAC.Read.All` | Intune RBAC role definitions and assignments (scope tag audit) |
| `SecurityEvents.Read.All` | Secure Score, improvement actions, security alerts |

**Exchange Online RBAC role groups**

| Role group | Reason |
|---|---|
| `Compliance Management` | Read access to compliance-related EXO configuration (journal rules, message tracking, transport compliance rules). |
| `View-Only Organization Management` | Read-only access to mailboxes, recipients, transport rules, connectors, and EOP/Defender policies. Replaces the on-prem-only "View-Only Recipients" and "View-Only Configuration" groups in cloud EXO. |

**Entra directory roles** (for Purview / Compliance access)

| Role | Reason |
|---|---|
| `Compliance Administrator` | Read access to Purview compliance configuration -- DLP policies, audit, retention, sensitivity labels. |
| `Global Reader` | Broad read-only access across Microsoft 365 services including Purview, covering gaps not addressed by the above roles. |
| `Security Reader` | Read access to Microsoft Defender and security-related settings, alerts, and policies. |

### SOC2

**Services touched:** Graph, Purview

**Delegated Microsoft Graph scopes**

- `AuditLog.Read.All`
- `Directory.Read.All`
- `Policy.Read.All`
- `Reports.Read.All`
- `RoleManagement.Read.Directory`
- `SecurityAlert.Read.All`
- `SecurityEvents.Read.All`
- `User.Read.All`

### Tenant

**Services touched:** Graph

**Delegated Microsoft Graph scopes**

- `Domain.Read.All`
- `Group.Read.All`
- `Organization.Read.All`
- `Policy.Read.All`
- `User.Read.All`

**Microsoft Graph application permissions** (app-only auth)

| Permission | Reason |
|---|---|
| `Domain.Read.All` | All domains registered in the tenant |
| `Group.Read.All` | All groups including Microsoft 365 and security groups |
| `Organization.Read.All` | Tenant org details, verified domains, hybrid config |
| `Policy.Read.All` | Conditional Access, auth methods, token lifetime, password policies |
| `User.Read.All` | User profiles, sign-in activity, license assignments |

### ValueOpportunity

**Services touched:** Graph

**Delegated Microsoft Graph scopes**

- `Organization.Read.All`

## Behavior when a permission is missing

When a Graph / EXO / Purview API call fails due to a missing permission, the responsible collector emits `Status='Unknown'` per `docs/CHECK-STATUS-MODEL.md`. The `CurrentValue` field surfaces what could not be collected, and `Remediation` calls out the exact missing permission so the user knows what to grant.

Concretely:

| Failure mode | Status emitted | Counted toward Pass% denominator? |
|---|---|---|
| Missing Graph scope (delegated) | Unknown | No |
| Missing Graph app role (app-only) | Unknown | No |
| EXO cmdlet not available (license/role) | NotLicensed | No |
| Tenant does not run the relevant service | NotApplicable | No |
| Section explicitly skipped via `-Section` filter | Skipped | No |
| Transient API error (timeout, throttle) | Unknown | No |

See `docs/CHECK-STATUS-MODEL.md` for the full status taxonomy and decision tree.

## Regenerating this document

```powershell
pwsh -NoProfile -File ./scripts/Build-PermissionsMatrix.ps1
```

Run this whenever you change the section-to-scope map (`AssessmentMaps.ps1`) or the permission definitions (`PermissionDefinitions.ps1`). Commit the regenerated `docs/reference/PERMISSIONS.md` alongside the source change. CI runs the script with `-Check` and fails the PR if the doc is out of sync.

