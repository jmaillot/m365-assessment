# Secrets — never commit
.env
.env.*
!.env.example

# Runtime data — Docker volume + local eval
data/
*.db
*.db-journal
*.db-wal
*.db-shm
legacy/
web-data/

# Dependencies — Docker builds from lockfile
node_modules/
.pnp/
web/node_modules/
web/.next/
web/dist/
dist/

# Python — CIS PDF → framework helper
.venv/
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE — keep minimal
.vscode/
.idea/
*.swp
*~

# Planning / AI — internal, not needed to run
.planning/
.claude/
.cursor/
.continue/
# Next.js agent rules — auto-generated by next dev, not needed in public repo
web/AGENTS.md
web/CLAUDE.md

# PS output (assessment runs, not source)
M365-Assessment/
/M365-Assess/
CIS/
SCF/
SecFrame/
*_transcript_*.txt

# Large PDF — keep locally, not in public repo (framework JSON already committed)
CIS_*.pdf
CIS_Microsoft_365*.pdf

# PS report build — not needed for Docker minimal (web uses Next.js)
babel.config.json

# Slim repo — PowerShell-era paths removed from git, keep out
docs/
tests/
Setup/
scripts/
CHANGELOG.md
CONTRIBUTING.md
SECURITY.md
THIRD-PARTY-LICENSES.md
PSScriptAnalyzerSettings.psd1

# Github workflows
.github/