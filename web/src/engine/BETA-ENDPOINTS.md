# Beta Endpoint Exceptions (D-15)

**Status: REVIEWED — 2026-08-26 (plan 02-12 task 5, D-15 sign-off)**

Graph endpoint policy: **v1.0 by default**. Any beta endpoint required by a
ported check lands on this documented exception list, approved at review.

Collector plans append one row here per retained beta endpoint. Each retention
requires: a checked v1.0 equivalent (or verified absence), a decision, and a
justification.

Review outcome (02-12): every row below carries a `promote-to-v1.0` or
`resolved — no call site` disposition; **zero `/beta/` call-site literals
remain in `web/src/engine/**`** (verified by source scan during the 02-12
read-only allowlist work). No endpoint was accepted as a retained beta
dependency, so no standing exception was granted.

| Beta endpoint | v1.0 equivalent checked? | Decision | Justification |
|---------------|--------------------------|----------|---------------|
| `/beta/reports/authenticationMethods/userRegistrationDetails` | ✓ yes: /v1.0/reports/authenticationMethods/userRegistrationDetails | promote-to-v1.0 | Resolved during plan 02-05 (mfa-report port): Get-MfaReport.ps1 targets v1.0 via Get-MgReportAuthenticationMethodUserRegistrationDetail; the TS port calls the v1.0 path directly. No beta surface remains for MFA registration details. |
| `/beta/organization/{tenantId}` | ✓ yes: /v1.0/organization/{id} | promote-to-v1.0 | Resolved during plan 02-07 (user-group-checks port): EntraUserGroupChecks.ps1 line 386 reads linkedInConfiguration off the beta organization entity; the v1.0 organization surface is used instead and an absent linkedInConfiguration degrades through the collector's existing null-guard to PS's default-enabled branch identically (no decision logic change). |
| `/beta/reports/authenticationMethods/userRegistrationDetails` (`$select` probe) | ✓ yes: /v1.0/reports/authenticationMethods/userRegistrationDetails | promote-to-v1.0 | Resolved during plan 02-07 (user-group-checks port): EntraUserGroupChecks.ps1 line 428 uses the beta path only as a discarded-result API-access probe emitting a Review row either way; same surface family already promoted to v1.0 during plan 02-05 (row 1). |
| `/beta/identityGovernance/accessReviews/definitions` | ✓ yes: verified absence | resolved — no call site | Resolved during plan 02-10 (ent-app-security-config port): `Get-EntAppSecurityConfig.ps1` contains ZERO `accessReviews` call sites (grep over all 1,150 lines — the planned D-15 candidate does not exist in the shipped collector; its 24 checks evaluate SPs/grants/roles/owners/app-registrations only). No beta surface introduced or retained by this collector. |
| `/beta/policies/deviceRegistrationPolicy` | ✓ yes: /v1.0/policies/deviceRegistrationPolicy | promote-to-v1.0 | Evaluated during plan 02-06 as mandated: NONE of the seven ported Identity/Licensing collectors contained a deviceRegistrationPolicy call site. Resolved during plan 02-08 (CA/device check-helper port): EntraConditionalAccessChecks.ps1 line 126 reads extended properties (azureADJoin.localAdmins.registeredUsers.additionalLocalAdminsCount, localAdminPassword.isEnabled) off the beta surface; the v1.0 deviceRegistrationPolicy is GA and its documented response carries localAdminPassword.isEnabled (Microsoft Learn v1.0 example), so absent/renamed extended fields degrade through the port's existing null-guards identically to PS behavior when beta omits them. TS port calls the v1.0 path for BOTH PS reads (second fetch preserved for call-count parity). |
| `/beta/roleManagement/directory/roleAssignmentScheduleInstances` | ✓ yes: /v1.0/roleManagement/directory/roleAssignmentScheduleInstances | promote-to-v1.0 | First evaluated during plan 02-06 (priv-remote port): Get-EntraPrivRemoteConfig.ps1 targets the v1.0 eligible-instances surface directly. Resolved during plan 02-08 (admin-role check-helper port): EntraAdminRoleChecks.ps1 line 124 consumes the beta assignment-instances path purely as a PIM availability probe whose result is discarded; the v1.0 list operation is GA (Microsoft Learn v1.0 reference, unifiedRoleAssignmentScheduleInstance) and the port calls it with RoleManagement.Read.Directory. No beta PIM surface remains. |
| `/beta/roleManagement/directory/roleEligibilityScheduleInstances` (EntraAdminRoleChecks.ps1 line 169) | ✓ yes: /v1.0/roleManagement/directory/roleEligibilityScheduleInstances | promote-to-v1.0 | Resolved during plan 02-08 (admin-role check-helper port): same eligibility-instances surface family already promoted during plan 02-06 (Get-EntraPrivRemoteConfig.ps1 uses the v1.0 path); the permanent-GA subtraction logic reads only principalId, identical in both versions. |
| `/beta/identityGovernance/accessReviews/definitions` | ✓ yes: /v1.0/identityGovernance/accessReviews/definitions | promote-to-v1.0 | Resolved during plan 02-08 (admin-role check-helper port): EntraAdminRoleChecks.ps1 line 232 lists review definitions ($top=100) for CIS 5.3.2/5.3.3 guest/privileged-role detection reading scope.query/scope.@odata.type only; the schedule-based accessReviews API is GA in v1.0 and those scope fields are version-stable. Note: plan 02-10's "verified absence" applied to Get-EntAppSecurityConfig.ps1 only — THIS collector does have the call site. |
| `/beta/deviceManagement/settings` | ✓ yes: /v1.0/deviceManagement/settings | promote-to-v1.0 | Resolved during plan 04-01 (intune-security-config port): Invoke-MgGraphRequest '/beta/deviceManagement/settings' → GET /v1.0/deviceManagement/settings (DeviceManagementConfiguration.Read.All). v1.0 document lists deviceComplianceCheckinThresholdDays; singleton shape handled in TS port. |
| `/beta/deviceManagement/deviceEnrollmentConfigurations` | ✗ none — no v1.0 equivalent | → keep | D-23 allowlist (beta→keep) — `Get-IntuneSecurityConfig.ps1` beta/deviceManagement/deviceEnrollmentConfigurations has no v1.0 promotion; declared as `/beta/deviceManagement/deviceEnrollmentConfigurations` in `SECTION_REGISTRY` and probed via `GET /beta/deviceManagement/deviceEnrollmentConfigurations?$top=1` in `verifyAppPermissions` (see `BETA-ENDPOINTS.md` at repo root). |
| `/beta/security/secureScores` | ✓ yes: /v1.0/security/secureScores | promote-to-v1.0 | Resolved during plan 04-01 (secure-score-report port): Get-MgSecuritySecureScore via /beta promoted — /v1.0/security/secureScores?$top=180 is GA and carries currentScore/maxScore/controlScores + AverageComparativeScores parity. |
| `/beta/security/secureScoreControlProfiles` | ✓ yes: /v1.0/security/secureScoreControlProfiles | promote-to-v1.0 | Resolved during plan 04-01 (secure-score-report + defender-security-config ports): /beta/security/secureScoreControlProfiles → GET /v1.0/security/secureScoreControlProfiles?$top=250 is GA (controlCategory/title/implementationStatus/actionType stable). |
| `/beta/informationProtection/sensitivityLabels` | ✓ yes: /v1.0/informationProtection/sensitivityLabels | promote-to-v1.0 | Resolved during plan 04-01 (compliance-security-config port): /beta/informationProtection/sensitivityLabels → GET /v1.0/informationProtection/sensitivityLabels (InformationProtectionPolicy.Read.All). Label list used for COMPLIANCE-LABELS checks. |
| `/beta/security/alerts_v2` | ✓ yes: /v1.0/security/alerts_v2 | promote-to-v1.0 | Resolved during plan 04-01 (compliance-security-config port): /beta/security/alerts_v2 → GET /v1.0/security/alerts_v2?$top=100 (SecurityAlert.Read.All). Alert status filtering for COMPLIANCE-ALERTPOLICY-001. |
| `/beta/admin/sharepoint/settings` | ✓ yes: /v1.0/admin/sharepoint/settings | promote-to-v1.0 | Evaluated during plan 05-01 (sharepoint-security-config port): PS Get-SharePointSecurityConfig.ps1 line 57 targets /v1.0/admin/sharepoint/settings directly — no beta surface retained. Verified no `/beta` literals remain in `web/src/engine/sections/collaboration/**`. |
| `/beta/teamwork/*` (teamsAppSettings, teamsClientConfiguration, teamsMeetingPolicy) | ✓ yes: /v1.0/teamwork + /v1.0/groupSettings | promote-to-v1.0 | Resolved during plan 05-01 (teams-security-config port): PS Get-TeamsSecurityConfig.ps1 used /beta/teamwork/teamsAppSettings and /beta/teamwork/teamsClientConfiguration + teamsMeetingPolicy; sovereign clouds return 400 on beta. TS port uses only v1.0: `GET /v1.0/teamwork` (TeamSettings.Read.All) and `GET /v1.0/groupSettings` for guest access. Beta-only meeting/client checks degrade to Skipped(not_implemented) with no `/beta` call site. |
| `/beta/admin/forms/settings` | ✓ yes: /v1.0/admin/forms/settings | promote-to-v1.0 | Resolved during plan 05-01 (forms-security-config port): PS Get-FormsSecurityConfig.ps1 used /beta/admin/forms/settings (OrgSettings-Forms.Read.All, #941). No v1.0 promotion served on 2026-08-28; TS port probes `GET /v1.0/admin/forms/settings` and degrades to Skipped with manual-verification note on 404/400 — zero `/beta` literals remain. Future promotion would be handled by the existing v1.0 probe without code change. |
| `/beta/security/labels/retentionLabels` | ✓ yes: /v1.0/security/labels/retentionLabels | promote-to-v1.0 | Verified during plan 06-01 (purview-retention-config port): PS `Purview/Get-PurviewRetentionConfig.ps1` uses IPPSSession `Get-RetentionCompliancePolicy` with no Graph beta surface; TS port calls only `GET /v1.0/security/labels/retentionLabels` (RecordsManagement.Read.All). Zero `/beta` literals in `web/src/engine/sections/purview/**` (06-02 verification). |
| `/beta/users`, `/beta/groups`, `/beta/sites/getAllSites`, `/beta/drives` (inventory) | ✓ yes: /v1.0/users, /v1.0/groups, /v1.0/sites/getAllSites, /v1.0/drives | promote-to-v1.0 | Verified during plan 06-01 (inventory ports): PS Inventory uses EXO cmdlets / reports with no beta Graph surface; TS ports use only v1.0 directory/sites/drives collections (`/v1.0/users`, `/v1.0/groups`, `/v1.0/sites/getAllSites`, `/v1.0/drives`). Zero `/beta` literals in `web/src/engine/sections/inventory/**` (06-02 verification). |
| `/beta/myorg/admin/tenantSettings` (Power BI) | ✓ yes: /v1.0/myorg/admin/tenantSettings (canonically `https://api.powerbi.com/v1.0/myorg/admin/tenantSettings`) | promote-to-v1.0 | Verified during plan 06-01 (powerbi-security-config port): PS `PowerBI/Get-PowerBISecurityConfig.ps1` uses `MicrosoftPowerBIMgmt` + `https://api.powerbi.com/v1.0/myorg/admin/tenantSettings`; TS port uses `GET /v1.0/myorg/admin/tenantSettings` (Tenant.Read.All) with no beta segment. Zero `/beta` literals in `web/src/engine/sections/powerbi/**` (06-02 verification). |

<!-- Template for new rows:
| /beta/... | ☐ pending / ✓ yes: /v1.0/... / ✗ none | retain / promote-to-v1.0 | why |
-->
